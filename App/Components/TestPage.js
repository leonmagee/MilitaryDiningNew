import React from 'react';
import { View, Text } from 'react-native';

const TestPage = () => {
  return (
    <View>
      <Text>Just a test page...</Text>
    </View>
  );
};

export default TestPage;
